# SamaPay

Key-scoped crypto deposit/withdrawal service for SamaPrime and its clients.
See `CLAUDE.md` for the rules and the spec pointer. Nothing here is running
yet: no database, no PM2 entry, no port — those are Ibrahim's keystrokes.
